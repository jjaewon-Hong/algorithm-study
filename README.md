# algorithm-study

Winter vacation weekly problem-solving study using Baekjoon Online Judge. (26.01.04 ~ 26.02.27)

## 🚀 Rules

* **Collaborative Learning**: Weekly problem-solving with C++ and JavaScript colleagues. (3 members total).
  
  **[Meetings : Wed & Sun]**
* **Peer Review**: Compare logic differences (Python vs. C++ vs. JS) and discuss optimizations.
* **No AI Assistance**: To strengthen pure problem-solving and thinking skills. But if no progress is made after at least an hour, reference is allowed.

## 🛠️ Structure

* **week01** : `1929` 소수 구하기, `2407` 조합, `10974` 모든 순열
* **week02** : `10828` 스택, `15649` N과 M(1), `15650` N과 M(2), `15651` N과 M(3), `2824` 최대공약수, `10845` 큐, `1935` 후위표기식
* **week03** : `1927` 최소 힙, `11729` 하노이 탑 이동 순서, `10814` 나이순 정렬, `10989` 수 정렬하기3, `1431` 시리얼 번호, `1003` 피보나치 함수, `11053` 가장 긴 증가하는 부분 수열
* **week04** : `1182` 부분수열의 합, `1021` 회전하는 큐, `1427` 소트인사이드, `1260` DFS와 BFS, `11724` 연결요소의 개수, `1991` 트리순회
* **week05** : `2178` 미로 탐색하기, `7576` 토마토, `5014` 스타트링크, `2512` 예산, `2630` 색종이 만들기, `2448` 별 찍기, `2606` 바이러스
* **week06** : `1992` 쿼드트리, `2110`  공유기 설치, `1783`  병든 나이트, `11501`  주식, `1946`  신입 사원, `11497` 통나무 건너뛰기
* **week07** : `2961` 도영이가 만든 맛있는 음식, `1713` 후보 추천하기, `3190` 뱀, `1541` 잃어버린 괄호, `16953` A → B, `1049` 기타줄, `11047` 동전 0
* **week08** : `13305` 주유소, `12789` 도키도키 간식드리미, `4963` 섬의 개수, `11725` 트리의 부모 찾기, `25418` 정수 a를 k로 만들기, `1966` 프린터큐, `1105` 팔
* **week09** : `2579` 계단오르기, `2193` 이친수, `15658` 연산자끼워넣기, `9020` 골드바흐의 추측, `1389` 케빈 베이컨의 6단계 법칙, `5567` 결혼식
